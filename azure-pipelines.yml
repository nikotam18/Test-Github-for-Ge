# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.11'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'package'

    - task: FortifyOnDemandStatic@9
      inputs:
        FortifyProjects: '$(Build.SourcesDirectory)'
        ScanCentralLocation: 'C:\Program Files\Fortify\Fortify_SCA_24.4.1\bin'
        FodConnection: 'Nikolasisi Global'
        ReleaseOptions: '0'
        ReleaseId: 197771
        EntitlementSelection: '1'
        EntitlementPreference: '0'
        OverrideScanSettings: '2'
        InProgressScanActionType: '2'
        RemediationScanPreference: '0'
        BuildType: 'none'
        PolicyFailAction: '1'
